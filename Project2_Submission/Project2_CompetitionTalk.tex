\documentclass[aspectratio=169,10pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,decorations.pathmorphing}
\usepackage{animate}
\graphicspath{{figures/}{figures/lesson/}{figures/gifs/}}
\newcommand{\norm}[1]{\lVert #1 \rVert}

\definecolor{osuorange}{HTML}{DC4405}
\definecolor{osudark}{HTML}{1A1A1A}
\definecolor{osugray}{HTML}{4D4D4D}

\usetheme{default}
\setbeamercolor{title}{fg=osuorange}
\setbeamercolor{frametitle}{fg=white,bg=osuorange}
\setbeamercolor{structure}{fg=osuorange}
\setbeamercolor{normal text}{fg=osudark}
\setbeamercolor{itemize item}{fg=osuorange}
\setbeamercolor{itemize subitem}{fg=osuorange}
\setbeamerfont{frametitle}{series=\bfseries,size=\large}
\setbeamerfont{title}{series=\bfseries}
\setbeamertemplate{itemize item}{\raisebox{0.25ex}{\footnotesize\textcolor{osuorange}{$\blacktriangleright$}}}
\setbeamertemplate{itemize subitem}{\textcolor{osuorange}{\textbullet}}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{footline}{%
  \hfill\textcolor{osugray}{\footnotesize\insertframenumber}\hspace{1.2em}\vspace{0.8em}}
\setbeamertemplate{frametitle}{%
  \vspace{0.4em}\hspace{0.2em}\textcolor{osuorange}{\bfseries\insertframetitle}\par
  \vspace{-0.2em}{\color{osuorange}\rule{\linewidth}{1.2pt}}}

% tighter list spacing
\setlength{\leftmargini}{1.2em}
\setbeamertemplate{itemize/enumerate body begin}{\itemsep=2pt}

\title{\textbf{Recovering the Hidden Digit, Part II}}
\subtitle{Diffusion-prior posterior sampling for ERT}
\author{Travis Whitney \quad Cole Seifert \quad Alexander Nutt}
\institute{AI 539, Oregon State University}
\date{Project 2 Competition, Spring 2026}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}

%==============================================================
\begin{frame}{Same problem, new rule}
The physics from Project 1 hasn't changed. We still have a hidden
$28 \times 28$ digit, a list of 1900 boundary voltage readings, and
the forward simulator $F$. The job is still to work backward from the
readings to the digit.

\vspace{0.6em}
What changed is the rule on the prior. In Project 1 we used the 60{,}000
MNIST training images as a lookup table. For Project 2 the prior has to
be a diffusion model. A diffusion model is a neural network that learned
what a digit looks like by practicing on the MNIST set. We can ask it to
draw a fresh digit, but it does not know about our voltage readings.

\vspace{0.6em}
The rest of this talk is how we combined a diffusion model with the ERT
measurements. The final pipeline uses a class-conditional diffusion
prior with classifier-free guidance and a total-variation polish. The
final misfit is
\textcolor{osuorange}{$\mathbf{1.83 \times 10^{-11}}$}, recovering a
clean tilted 5.
\end{frame}

%==============================================================
\begin{frame}{The pipeline}
\centering
\begin{tikzpicture}[node distance=0.4cm and 1.0cm, every node/.style={align=center}]
\node[draw, rounded corners, fill=orange!15, minimum width=3.0cm, minimum height=1.2cm,
      font=\footnotesize\bfseries] (A) {Stage A\\class-cond.\\rotation-aug.\\DDPM};
\node[draw, rounded corners, fill=orange!25, minimum width=3.0cm, minimum height=1.2cm,
      right=of A, font=\footnotesize\bfseries] (B) {Stage B\\PnP-DM\\with CFG\\32 chains};
\node[draw, rounded corners, fill=orange!40, minimum width=3.0cm, minimum height=1.2cm,
      right=of B, font=\footnotesize\bfseries] (C) {Stage C\\TV-regularized\\polish};
\draw[-{Latex[length=3mm]}, very thick, osuorange] (A) -- (B);
\draw[-{Latex[length=3mm]}, very thick, osuorange] (B) -- (C);
\node[below=0.4cm of A, font=\scriptsize, color=osugray] {teach the prior\\digit class + tilt};
\node[below=0.4cm of B, font=\scriptsize, color=osugray] {draw 32 candidate\\images per class};
\node[below=0.4cm of C, font=\scriptsize, color=osugray] {tighten fit without\\adding speckle};
\end{tikzpicture}

\vspace{1.5em}
Stage A is a one-time setup. After that, every run goes through Stage B
and Stage C. Total wall time on a MacBook is about eight minutes.
\end{frame}

%==============================================================
\begin{frame}{What a diffusion model lets us do}
\begin{columns}[T]
\column{0.55\textwidth}
\vspace{0.4em}
{\small A diffusion model is trained to look at a noisy image and predict
the noise that was added. Once it can do that, simple algebra gives us a
guess at the clean image underneath:}
\[
\hat x_0 \;=\;
\frac{x_t \,-\, \sqrt{1 - \bar\alpha_t}\,\varepsilon_\theta(x_t, t)}{\sqrt{\bar\alpha_t}}.
\]
\vspace{-0.3em}

{\small So at any moment along a noisy chain, the network gives us its
best guess at the clean digit underneath. At high noise the guess is
fuzzy. At low noise it is sharp. Every method here leans on that one
identity.}

\column{0.45\textwidth}
\centering
\includegraphics[width=\linewidth]{fig05_tweedie_strip.png}\\
{\scriptsize\color{osugray} Top: a noisy input at four noise levels.
Bottom: the network's clean-image guess at each.}
\end{columns}
\end{frame}

%==============================================================
\begin{frame}{What we tried first, and why none of it worked}
\scriptsize
\begin{columns}[T]
\column{0.58\textwidth}

We ran five diffusion-based recipes before landing on the method in
this talk.

\vspace{0.3em}
\begin{itemize}\itemsep=2pt
\item \textbf{DPS} (Diffusion Posterior Sampling). Nudge the image by
  the data gradient at every denoising step. \textit{Result: 4 of 5
  chains converged to a 4. Wrong digit every time.}
\item \textbf{DAPS} (Decoupled Annealing Posterior Sampling). Diffusion
  step for class and shape, separate Langevin step for the data.
  \textit{Result: still mostly 4s. Same weak data step as DPS.}
\item \textbf{DI-RTG v1} (DAPS warm-start + MNIST classifier vote +
  Levenberg-Marquardt polish). \textit{Result: classifier voted ``4''
  too. Polish drove a wrong-digit answer to high precision.}
\item \textbf{DI-RTG v2} (DAPS warm-start + template retrieval).
  \textit{Result: nearest training image was always upright, truth is
  tilted by 7.5 degrees.}
\item \textbf{Base PnP-DM + Newton polish} (variable splitting,
  rotation-augmented DDPM, Gauss-Newton data step, Newton polish to
  float64 floor). \textit{Result: correct digit, but the polish
  drives the misfit to $\sim 10^{-13}$ by fitting round-off noise as
  per-pixel speckle. Clean math, ugly picture.}
\end{itemize}

\column{0.42\textwidth}
\centering
\includegraphics[width=\linewidth]{dps_chains.png}\\
{\scriptsize\color{osugray} The DPS run that started it all. Eight
chains, each locking onto a wrong digit (3, 4, 6, 8). Not a 5 in sight.}

\vspace{0.4em}
\begin{center}
{\footnotesize\textcolor{osuorange}{\textbf{Two failure modes.}}}\\
{\scriptsize\color{osugray}
The first four pick the \emph{wrong digit} because the data step is
too weak. The base PnP-DM picks the right digit but produces an
\emph{ugly picture} because the Newton polish over-fits round-off noise.
Our method has to fix both.}
\end{center}
\end{columns}
\end{frame}

%==============================================================
\begin{frame}{Our method: three pieces working together}
\footnotesize
We need a sampler that picks the \emph{right} digit \emph{and} produces
a clean image. The method has three pieces; each one fixes one specific
failure of the recipes on the previous slide.

\vspace{0.6em}
\begin{itemize}\itemsep=6pt
\item \textbf{Variable splitting (PnP-DM).} Give each pull its own
  variable: $x$ has to look like a digit, $z$ has to fit the data,
  and a soft penalty keeps them close. Alternate. The data update can
  use the full Jacobian via one Gauss-Newton solve, so it has real
  teeth. \textit{Fixes: weak-data-step problem (DPS, DAPS).}
\item \textbf{Class-conditional prior with classifier-free guidance.}
  Train the diffusion model with the digit label as an extra input.
  At inference, each chain is seeded to a specific class and uses CFG
  to commit to that class instead of waiting for the data step to roll
  it into a valley. \textit{Fixes: wrong-digit-basin problem (all four
  early attempts).}
\item \textbf{TV-regularized polish.} Replace the plain Newton polish
  with a Newton step plus a smoothed total-variation gradient step at
  every iteration. The TV term penalizes per-pixel variation, so the
  polish cannot fit round-off noise as speckle. \textit{Fixes:
  speckle-in-the-picture problem (base PnP-DM).}
\end{itemize}

\vspace{0.5em}
The next slides develop each piece, then we put them together and look
at the result.
\end{frame}

%==============================================================
\begin{frame}{Piece 1 --- Variable splitting (PnP-DM)}
\begin{columns}[T]
\column{0.55\textwidth}
\vspace{0.2em}
\footnotesize
DPS asks one variable to do two jobs at once: look like a digit, and
fit the data. The two pulls compromise on every step, and the smaller
one loses.

\vspace{0.5em}
Instead, introduce a second variable. Call them $x$ and $z$:

\vspace{0.3em}
\begin{itemize}\itemsep=3pt
\item $x$ only has to look like a digit (the prior touches it).
\item $z$ only has to fit the measurements (the likelihood touches it).
\item A soft penalty $\tfrac{1}{2\sigma_n^2}\|z-x\|^2$ keeps them close.
\end{itemize}

\vspace{0.5em}
Then alternate. Update $z$ to fit the data. Update $x$ to look like a
digit. Repeat. Each update does one thing well. This sampler is
called PnP-DM (Wu et al., NeurIPS 2024). We run 80 alternations per
chain, 32 chains in parallel.

\column{0.45\textwidth}
\centering
\begin{tikzpicture}[scale=0.95]
\node[draw, circle, fill=red!20, minimum size=0.9cm] (x1) at (0, 0) {\(x\)};
\node[draw, circle, fill=blue!20, minimum size=0.9cm] (z1) at (2.4, 0) {\(z\)};
\node[draw, circle, fill=red!20, minimum size=0.9cm] (x2) at (4.8, 0) {\(x\)};
\draw[-{Latex[length=2mm]}, thick, blue!70!black]
  (x1.north) .. controls (0.6, 1.2) and (1.8, 1.2) .. (z1.north);
\node[blue!60!black, font=\scriptsize, align=center] at (1.2, 1.4)
  {fit data};
\draw[-{Latex[length=2mm]}, thick, red!70!black]
  (z1.north) .. controls (3.0, 1.2) and (4.2, 1.2) .. (x2.north);
\node[red!60!black, font=\scriptsize, align=center] at (3.6, 1.4)
  {look like digit};
\draw[gray!60, decorate, decoration={snake, amplitude=0.5mm, segment length=2mm}] (x1.south) -- (z1.south);
\draw[gray!60, decorate, decoration={snake, amplitude=0.5mm, segment length=2mm}] (z1.south) -- (x2.south);
\node[font=\tiny, gray] at (2.4, -0.8) {coupled by slack \(\sigma_n\)};
\end{tikzpicture}
\end{columns}
\end{frame}

%==============================================================
\begin{frame}{What the data step actually minimizes}
\footnotesize
The data step picks the $z$ that does two things at once: fits the
measurements, and stays close to the current $x$. Written as an
objective:
\[
\mathcal{L}(z)
\;=\;
\underbrace{\tfrac{1}{2\sigma_{\text{meas}}^2}\,\norm{y_{\text{obs}} - F(\sigma_{\text{bg}} + z)}^2}_{\textcolor{osuorange}{\text{data misfit}}}
\;\;+\;\;
\underbrace{\tfrac{1}{2\sigma_n^2}\,\norm{z - x}^2}_{\textcolor{osuorange}{\text{coupling penalty}}}.
\]
\vspace{-0.3em}

\textbf{Piece by piece.}
\begin{itemize}\itemsep=2pt
\item $y_{\text{obs}}$ is the 1900 measured voltages.
$F(\sigma_{\text{bg}} + z)$ is what the simulator would predict for our
candidate. The squared norm is the sum of per-electrode errors.
\item $\sigma_{\text{meas}}$ is how noisy we think the measurements
are. Smaller $\sigma_{\text{meas}}$ means we trust them more, so the
first term gets a bigger weight.
\item $\norm{z - x}^2$ pulls $z$ back toward the current $x$. The
weight $1/\sigma_n^2$ depends on the slack: tight slack means strong
pull.
\end{itemize}

\textbf{Reading it.} ``Find a $z$ that explains the measurements, but
do not stray too far from the current digit guess.'' At the start of
the chain $\sigma_n$ is big (loose leash), so the data wins. As the
schedule anneals, $\sigma_n$ shrinks and the coupling wins, forcing
$z$ and $x$ to agree.
\end{frame}

%==============================================================
\begin{frame}{How we solve it: one Gauss-Newton step}
\footnotesize
DPS takes a small step in the gradient direction. We instead linearize
$F$ around the current $z$, plug the linearization into $\mathcal{L}$,
and minimize the resulting quadratic. The minimum is the solution of a
small linear system in the update $\Delta$:
\[
\underbrace{\Bigl(\tfrac{1}{\sigma_{\text{meas}}^2} J^\top J + \tfrac{1}{\sigma_n^2} I\Bigr)}_{\textcolor{osuorange}{\text{curvature, with damping}}}
\;\Delta
\;=\;
\underbrace{-\tfrac{1}{\sigma_{\text{meas}}^2} J^\top r}_{\textcolor{osuorange}{\text{data pull}}}
\;+\;
\underbrace{\tfrac{1}{\sigma_n^2}(x - z)}_{\textcolor{osuorange}{\text{coupling pull}}}.
\]
\vspace{-0.3em}

\textbf{Piece by piece.}
\begin{itemize}\itemsep=2pt
\item $\Delta$ is the update we are solving for. After solving:
  $z \leftarrow z + \Delta$.
\item $J^\top J$ is the curvature of the data term. In directions
  where $J$ has big columns (sensitive pixels), the curvature is big
  and the step is small. The $\sigma_n^{-2} I$ damping fills in the
  directions $J$ cannot see, so the matrix is always invertible.
\item $r = F(\sigma_{\text{bg}} + z) - y_{\text{obs}}$ is the current
  residual. Multiplying by $J^\top$ back-projects it onto pixels:
  ``which pixels would reduce this residual.''
\item $(x - z)$ is the coupling restoring force: pulls $z$ back toward
  the current $x$.
\end{itemize}

\centering
\textcolor{osuorange}{One Gauss-Newton step does the work of dozens of gradient steps on ill-conditioned $J$.}
\end{frame}

%==============================================================
\begin{frame}{Piece 2 --- Class-conditional prior + classifier-free guidance}
\begin{columns}[T]
\column{0.50\textwidth}
\scriptsize
\textbf{Train the prior on rotated MNIST with class labels.} A
6-million-parameter UNet, trained from scratch on $[-15^\circ,
+15^\circ]$ rotated MNIST images paired with their digit class.
EMA-weighted parameters during training. 30 epochs on a 5090
($\sim 15$ minutes). The model now believes tilted digits are
realistic and can render any specific class on demand.

\vspace{0.3em}
\textbf{Classifier-free guidance.} During training, 10\% of labels are
replaced by a special unconditional token, so the same network learns
both $\varepsilon_\theta(x_t, t, c)$ and
$\varepsilon_\theta(x_t, t, \varnothing)$. At inference we combine:
\[
\varepsilon_{\text{guided}}
=
(1+w)\,\varepsilon_\theta(x_t, t, c)
-
w\,\varepsilon_\theta(x_t, t, \varnothing),
\]
with $w = 3$. Strong class commitment, no extra parameters.

\vspace{0.3em}
\textbf{32 chains, 10 class hypotheses.} Each chain is seeded to a
target class label that cycles through $0,\dots,9$, so every digit
gets at least three chains. The chain whose final misfit is lowest
wins. The sampler can now commit to a digit \emph{early}, instead of
waiting for the data gradient to roll it into a valley.

\column{0.50\textwidth}
\centering
\includegraphics[width=\linewidth]{fig16_cond_samples.png}\\[0.2em]
{\scriptsize\color{osugray} One unconditional sample per class from the
trained class-conditional model with $w = 3$. Each digit comes out in
its own slightly-tilted style.}
\end{columns}
\end{frame}

%==============================================================
\begin{frame}{Piece 3 --- Total-variation polish}
\begin{columns}[T]
\column{0.55\textwidth}
\footnotesize
The Newton-style polish minimizes
$\tfrac{1}{2}\norm{F(\sigma_{\text{bg}}+x) - y_{\text{obs}}}^2$. The TV
polish adds a smoothed total-variation penalty:
\[
\tfrac{1}{2}\norm{F(\sigma_{\text{bg}}+x) - y_{\text{obs}}}^2
\;+\;
\tau \sum_{i,j} \sqrt{(\partial_x x)^2 + (\partial_y x)^2 + \epsilon^2}.
\]
\vspace{-0.4em}

The second term counts the total horizontal and vertical pixel-to-pixel
variation. Speckle adds a lot of variation; smooth strokes add little.
At $\tau \approx 10^{-5}$ the TV term is small enough that the misfit
still drops to the $10^{-11}$ regime, but large enough that the polish
is no longer allowed to add per-pixel noise.

\vspace{0.5em}
Each polish iteration is one Newton step on the data term followed by
one gradient step on the TV term. The line search still gates updates,
so the misfit never increases.

\column{0.45\textwidth}
\centering
\includegraphics[width=\linewidth]{fig15_tv_vs_lm.png}\\[0.1em]
{\scriptsize\color{osugray} The TV polish keeps the strokes clean and
the background flat while still hitting low misfit.}
\end{columns}
\end{frame}

%==============================================================
\begin{frame}{One chain, six frames: noise becomes a 5}
\centering
\includegraphics[width=0.98\linewidth]{fig_denoising_progression.png}

\vspace{0.8em}
{\footnotesize
\textbf{Reading left to right.} A single chain of the full method
(class-conditional CFG sampler, target class 5). At iteration 1 the
image is essentially random noise. By iteration 5 a digit shape is
forming. By iteration 26 the digit identity is settled. By iteration
80 the chain has converged to a clean tilted 5 with pre-polish misfit
around $10^{-6}$. The TV polish then drives the misfit the rest of
the way to $1.83 \times 10^{-11}$.}
\end{frame}

%==============================================================
\begin{frame}{Same chain, animated: watch the digit class lock in}
\begin{columns}[T]
\column{0.40\textwidth}
\centering
\animategraphics[autoplay,loop,width=\linewidth]{8}{frames_competition/frame_}{0}{80}

\column{0.60\textwidth}
\vspace{0.1em}
\footnotesize
The same chain animated through all 80 iterations.

\vspace{0.4em}
\textbf{What to watch for.} In the first $\sim 5$ iterations the image
is still essentially noise. Around iteration 10 to 15 the
\textbf{data step} (Gauss-Newton) has accumulated enough information
about the voltages that the iterate starts to look like a coherent
shape. The class label $c = 5$ (via CFG) keeps the prior step pulling
toward a 5, so the chain commits to the right digit early. By
iteration 20 the digit class is locked in. From there on, the prior
step polishes the strokes and the data step nudges the pixels for a
tighter fit. The TV polish (a separate stage, not shown here) takes
the final image from misfit $\sim 10^{-6}$ to $\sim 10^{-11}$.

\vspace{0.4em}
\textcolor{osuorange}{The digit identity is decided early. The rest is refinement.}
\end{columns}
\end{frame}

%==============================================================
\begin{frame}{Where each chain starts, and why we run 32 of them}
\footnotesize
\textbf{Where does a chain start?} Not from the measurements. Each
chain starts from a fresh draw of pure Gaussian noise: $x_0 \sim
\mathcal{N}(0, I)$. That is just a $28 \times 28$ grid of random
numbers (TV static). The measurements $y_{\text{obs}}$ enter only
through the data step, which runs at every iteration.

\vspace{0.5em}
\textbf{Why several different chains can give different digits.} The
forward map is a $784 \to 1900$ blur. Two different digits can produce
voltage readings that agree to many decimal places. The posterior has
several valleys, one per plausible digit.

\vspace{0.5em}
\textbf{How CFG handles this.} Instead of letting the random starting
noise decide which valley each chain rolls into, we \emph{seed} each
chain to a target class label. With 32 chains and 10 classes, every
digit gets at least three chains, and the chains targeting the right
class have a strong head start at every denoising step. The chain
whose final data misfit is lowest wins. The 5 wins every time.

\vspace{0.5em}
\centering
\textcolor{osuorange}{32 chains $\times$ 10 class hypotheses
is the safety net. The lowest-misfit chain wins.}
\end{frame}

%==============================================================
\begin{frame}{The 12-combination sweep}
\begin{columns}[T]
\column{0.42\textwidth}
\footnotesize
We tested all $3 \times 2 \times 2 = 12$ combinations:

\vspace{0.4em}
\begin{itemize}\itemsep=2pt
\item \textbf{Prior:} base / bigger+EMA / class-conditional+CFG
\item \textbf{Schedule:} geomspace / DAPS++ negative-rho
\item \textbf{Polish:} Newton / TV-regularized
\end{itemize}

\vspace{0.5em}
Two things popped out.

\vspace{0.3em}
\begin{itemize}\itemsep=2pt
\item \textbf{TV polish beat Newton polish in every prior-schedule pair.}
  Uniformly lower misfit \emph{and} cleaner image.
\item \textbf{The class-conditional prior plus the geomspace schedule
  plus the TV polish was the lowest misfit overall.} $1.83 \times
  10^{-11}$, with a clean recognizable 5.
\end{itemize}

\vspace{0.4em}
The bottom-right two cells (class-cond + neg-rho) actually converged
to a class-6-shaped image. That is the schedule pushing the chain
into a different mode --- the same ambiguity the four failed methods
hit, surfacing again under a worse schedule.

\column{0.58\textwidth}
\centering
\includegraphics[width=\linewidth]{fig14_sweep_grid.png}
\end{columns}
\end{frame}

%==============================================================
\begin{frame}{The winner: cond + geomspace + TV polish}
\begin{columns}[T]
\column{0.50\textwidth}
\centering
\includegraphics[width=\linewidth]{fig13_base_vs_winner.png}

\column{0.50\textwidth}
\vspace{0.4em}
\footnotesize
The left image is the base pipeline (PnP-DM + Newton polish), misfit
$4.7 \times 10^{-11}$. Notice the stroke speckle and the background
texture.

\vspace{0.4em}
The right image is the new pipeline (class-conditional PnP-DM with
CFG + TV polish), misfit
\textcolor{osuorange}{$\boldsymbol{1.83 \times 10^{-11}}$}. The
strokes are smooth and the background is essentially flat.

\vspace{0.4em}
\textbf{Compared to Project 1's rotation-aware result on the same
data:} our new pipeline is roughly $\mathbf{5{,}400 \times}$ tighter
in misfit, and (qualitatively) a much sharper image.

\vspace{0.4em}
{\color{osugray} The improvement comes from the prior and the polish
acting together. Each one alone gives a modest gain; together they
remove both the speckle and the stroke darkness simultaneously.}
\end{columns}
\end{frame}

%==============================================================
\begin{frame}{What we recovered}
\begin{columns}[T]
\column{0.55\textwidth}
\centering
\includegraphics[width=\linewidth]{fig12_recovered_sigma.png}
\column{0.45\textwidth}
\vspace{0.8em}
\footnotesize
The recovered digit is a \textbf{5}, tilted by about $7.5$ degrees,
with smooth strokes and a clean background. The final misfit is
\textcolor{osuorange}{$\boldsymbol{1.83 \times 10^{-11}}$}.

\vspace{0.5em}
That is roughly $\mathbf{5{,}400}$ times tighter than Project 1's
rotation-aware result on the same data, recovered without a template
database, without a classifier vote, and without any prior knowledge
of the digit class.

\vspace{0.5em}
{\color{osugray} The diffusion does the heavy lifting: it puts the
chain into the right digit basin via class conditioning, and the
TV polish tightens the fit without amplifying noise.}
\end{columns}
\end{frame}

%==============================================================
\begin{frame}{Held-out validation: 9 out of 10 on the base pipeline}
\centering
\includegraphics[width=0.95\linewidth]{validate_final.png}

\vspace{0.4em}
{\footnotesize
Labels read \textbf{true to recovered}. For each test digit we
synthesized fresh measurements, ran the full pipeline, and asked a
classifier to read the recovered image. These ten cases were run on
the \emph{base} pipeline. The one miss (3 read as 5) is a genuine
ambiguity of the physics: that particular handwritten 3 has a curled
bottom that produces voltage readings extremely close to those of
certain 5s. The other nine cases polish to the same low-$10^{-11}$
regime as the competition vector.}
\end{frame}

%==============================================================
\begin{frame}{Three of those chains, animated, in parallel}
\centering
\begin{tabular}{ccc}
\animategraphics[autoplay,loop,width=0.24\linewidth]{8}{frames_digit1/frame_}{0}{80} &
\animategraphics[autoplay,loop,width=0.24\linewidth]{8}{frames_digit7/frame_}{0}{80} &
\animategraphics[autoplay,loop,width=0.24\linewidth]{8}{frames_digit8/frame_}{0}{80} \\[0.2em]
{\footnotesize held-out 1} & {\footnotesize held-out 7} & {\footnotesize held-out 8}
\end{tabular}

\vspace{0.8em}
{\footnotesize
Three different held-out test images, three independent chains from
the new class-conditional CFG sampler (each chain seeded to its
target class: 1, 7, 8). Each chain starts from its own fresh Gaussian
noise. Notice the common pattern: in the first $\sim 15$ iterations
the chain commits to a digit class, and the remaining $\sim 65$
iterations sharpen the strokes and drive the misfit down. There is
no template database anywhere in the pipeline. The class-conditional
diffusion prior and the Gauss-Newton data step do all the work.}
\end{frame}

%==============================================================
\begin{frame}{Summary}
\footnotesize
\begin{itemize}\itemsep=4pt
\item Four standard diffusion-based recipes (DPS, DAPS, classifier-vote,
  template retrieval) all committed to the wrong digit because their
  data step is too weak.
\item Variable splitting separates the two jobs and the Gauss-Newton
  solve gives the data step real teeth, so it can push the chain out
  of a wrong-digit basin. 32 parallel chains cover the posterior modes.
\item Rotation augmentation is a one-time fix that unlocks the
  diffusion prior for tilted digits.
\item On top of the base PnP-DM sampler we layered three improvements:
  TV-regularized polish, a bigger DDPM trained with EMA, and a
  class-conditional DDPM with classifier-free guidance. We swept all
  $12$ combinations to find the winner.
\item \textbf{Result:} digit \textbf{5}, final misfit
  \textcolor{osuorange}{$\mathbf{1.83 \times 10^{-11}}$}
  (about $5{,}400 \times$ tighter than Project 1's
  rotation-aware result), with clean strokes and a flat background.
  Base pipeline held-out: \textbf{9 out of 10}.
\end{itemize}

\vspace{0.6em}
\begin{center}
{\large\textcolor{osuorange}{\textbf{Thank you. Questions?}}}
\end{center}
\end{frame}

%==============================================================
\begin{frame}{References}
\scriptsize

\textbf{The method we used}
\begin{itemize}\itemsep=1pt
\item \textbf{PnP-DM.} Wu, Wang, Lin, Sun, \emph{Principled Probabilistic
Imaging Using Diffusion Models as Plug-and-Play Priors}, NeurIPS 2024.
arXiv:2405.18782.
\item \textbf{DDPM.} Ho, Jain, Abbeel, \emph{Denoising Diffusion
Probabilistic Models}, NeurIPS 2020. arXiv:2006.11239.
\item \textbf{Classifier-free guidance.} Ho, Salimans,
\emph{Classifier-Free Diffusion Guidance}, NeurIPS workshop 2021.
arXiv:2207.12598.
\item \textbf{EMA for diffusion training.} Karras, Aittala, Aila,
Laine, \emph{Elucidating the Design Space of Diffusion-Based Generative
Models}, NeurIPS 2022 (EDM). arXiv:2206.00364.
\item \textbf{Total-variation regularization.} Rudin, Osher, Fatemi,
\emph{Nonlinear total variation based noise removal algorithms},
Physica D, 1992.
\item \textbf{Tweedie's formula.} Efron, \emph{Tweedie's Formula and
Selection Bias}, JASA 2011.
\item \textbf{Pretrained MNIST DDPM.} \texttt{1aurent/ddpm-mnist} on
Hugging Face (the checkpoint we fine-tuned for the base prior).
\end{itemize}

\textbf{Methods we tested or compared against}
\begin{itemize}\itemsep=1pt
\item \textbf{DPS.} Chung, Kim, Mccann, Klasky, Ye, \emph{Diffusion
Posterior Sampling for General Noisy Inverse Problems}, NeurIPS 2022.
arXiv:2209.14687.
\item \textbf{DAPS.} Zhang, Chu, Berner, Meng, Anandkumar, Song,
\emph{Improving Diffusion Inverse Problem Solving with Decoupled Noise
Annealing}, CVPR 2025. arXiv:2407.01521.
\item \textbf{Classifier guidance.} Dhariwal, Nichol, \emph{Diffusion
Models Beat GANs on Image Synthesis}, NeurIPS 2021. arXiv:2105.05233.
\item \textbf{InverseBench.} Zheng et al., \emph{InverseBench: Benchmarking
Plug-and-Play Diffusion Models for Inverse Problems in the Physical
Sciences}, ICLR 2025. arXiv:2503.11043.
\end{itemize}

\textbf{Classical numerics (the polish) and dataset}
\begin{itemize}\itemsep=1pt
\item \textbf{Conjugate gradient.} Hestenes, Stiefel, \emph{Methods of
Conjugate Gradients for Solving Linear Systems}, J.~Res.~NBS, 1952.
\item \textbf{Gauss-Newton / Levenberg-Marquardt.} Nocedal, Wright,
\emph{Numerical Optimization}, 2nd ed., Springer, 2006.
\item \textbf{ERT2D simulator.} Provided by Prof.~Aghasi (course material).
\item \textbf{MNIST.} LeCun, Cortes, Burges, \emph{The MNIST Database of
Handwritten Digits}.
\end{itemize}
\end{frame}

\end{document}
